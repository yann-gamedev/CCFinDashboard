{
  "name": "ccfin-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "type-check": "npm run type-check --prefix client",
    "install:client": "npm install --prefix client"
  }
}
